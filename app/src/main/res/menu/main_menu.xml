<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/menu_back"
        android:title="Back"
        android:icon="@drawable/ic_arrow_back"
        app:showAsAction="ifRoom|withText"
        tools:ignore="HardcodedText" />

    <item
        android:id="@+id/menu_add_icon"
        android:title="Thêm mới"
        android:icon="@drawable/ic_circle_plus"
        app:showAsAction="ifRoom|withText"
        tools:ignore="HardcodedText" />

    <item android:id="@+id/menu_add" android:title="@string/menu_add" />
    <item android:id="@+id/menu_search" android:title="@string/menu_search" />
    <item android:id="@+id/menu_sort" android:title="@string/menu_sort" />
</menu>